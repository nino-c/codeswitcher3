

export default class InstanceToolsController {
    constructor($rootScope, $scope, $location) {
        'ngInject';

        this.initPanel = function() {

        }

        this._nextApp = function() {
            console.log($scope);
        }
    }
}
